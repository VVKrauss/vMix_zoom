# Для CLI: `supabase link` / `supabase db push` (ref из Supabase Dashboard).
project_id = "dbhrmaabotdaagmiuzum"

[db]
major_version = 17

# Вызов только из Database Webhook (Bearer WEBHOOK_PUSH_SECRET), без JWT пользователя.
[functions.send-dm-webpush]
verify_jwt = false
