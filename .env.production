# Подхватывается автоматически при `vite build` (даже без локального .env).
# Статика на redflow.online; Socket.IO и API — на этом хосте.
VITE_SIGNALING_URL=https://s.redflow.online
VITE_DEFAULT_ROOM=test
