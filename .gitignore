node_modules/
.env
dist/
supabase/.temp/

# Backend copy from server (local only)
# backend/  <-- больше не игнорируем: backend живёт в репозитории
backend/.env
backend/.env.*.local
backend/dist/
backend/node_modules/

# Локальные артефакты агента / архивы — не коммитить
agent-tools-deploy-payload.json
vMix replacer.zip
