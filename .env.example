# База для Socket.IO и REST /api/frontend/* (без завершающего слэша).
# Статика может жить на одном домене (например redflow.online), а API — на другом (например s.redflow.online).
VITE_SIGNALING_URL=https://s.redflow.online
VITE_DEFAULT_ROOM=test
